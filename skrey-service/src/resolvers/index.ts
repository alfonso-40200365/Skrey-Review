import { FeedbackResolver } from './FeedbackResolver' 
import { StoreResolver } from './StoreResolver'  
import { ClientResolver } from './ClientResolver' 
import { ProductResolver } from './ProductResolver' 
import { PurchaseResolver } from './PurchaseResolver' 

export const resolvers = [FeedbackResolver, StoreResolver, ProductResolver, ClientResolver, PurchaseResolver] 
